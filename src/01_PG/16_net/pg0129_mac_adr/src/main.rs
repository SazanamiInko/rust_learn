/////////////////////////////////
/// 
/// MACアドレス
/// 
/// /////////////////////////////

///メイン関数
fn main() {
    // ネットワークインターフェースを取得
    for x in get_if_addrs::get_if_addrs().unwrap() {
        if x.ip().to_string()=="127.0.0.1"{
            println!("{}", x.name);
        }
    }
}