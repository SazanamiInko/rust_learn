use std::path;

/////////////////////////////////////////////
/// 
/// 売り上げ
/// 
/// /////////////////////////////////////////
mod locker;

pub fn insert_uriage(path:&str)
{
    //開閉記録を読み込む
    let openlog=locker::load(path);
}